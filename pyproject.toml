[tool.pdm]

[project]
name = "mondot_shell"
version = "2.0.0"
description = "Shell for Mondot GUI"
authors = [
    {name = "thiagola92", email = "thiagola92@gmail.com"},
]
dependencies = [
    "pymongo>=4.3.3",
    "cx-Freeze>=6.14.9",
]
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.distutils.build_exe]
excludes = ["cx-Freeze"]
zip_include_packages = ["pymongo"]